'''python  modules types:
      Built-in modules:
      standard modules
      third party module
      user defined module
      '''

'''numeric module part 1:

1.NUMERIC MODULE:

the number module defines the numeric heirarchy(also known as the "Numeric Tower").
it uses abstarct base classes to categorize numbers,

The Hierarchy:
Number: The root heirarchy
complex:number with real and imaginary parts.
real:floating point numbers
rational:numbers can be expressed as fraction
integral:whole integer(int)


2.MATH MODULE:
    math module provides access to the mathematical functions defined by the C standard.

key features:
performance: functions are implemented in c,making them extremely fast.
precision:handles IEEE 754 floating-point arthimatic.
safety: provides tools to handle "not a number" (NaN) and  infinity.

3.MATHEMATICAL FUNCTIONS:
  A.power and logarthemic functions:
    used extensively in finance (compound interest) and data science.
    math.exp(x):returns e to the power 2.
    math.log(x,[base]): returns the logarthemic of x
    math.sqrt(x) return the square root of x.

  B.reprsentation functions:
    used for rounding and sign manipulation i UI\UX and billing systems.
    math.cell(x),math.floor(x),math.isclose(a,b):check 2 values nearly='''




'''Example:claculating the hypotgenuse of a support beam i construction using
pythagorean theorem:'''
import math














    

